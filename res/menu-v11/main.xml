<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/menu_search"
        android:showAsAction="ifRoom"
        android:icon="@drawable/ic_menu_search"
        android:title="@string/menu_search_title" />
    <!-- No showAsAction so it's added to the overflow menu -->
    <item android:id="@+id/menu_settings"
        android:icon="@drawable/action_settings"
        android:title="@string/action_show_settings" />
    <item android:id="@+id/menu_download"
        android:title="Download test" />
    <item android:id="@+id/menu_show_downloads"
        android:title="Show downloads" />
</menu>
